const $=s=>document.querySelector(s);let rooms=[];const money=n=>new Intl.NumberFormat('es-CO',{style:'currency',currency:'COP',maximumFractionDigits:0}).format(n);async function loadRooms(){rooms=await (await fetch('/api/public/rooms')).json();renderRooms(rooms)}function renderRooms(list){$('#roomGrid').innerHTML=list.map(r=>`<article class="room-card"><div class="room-visual"></div><div class="room-body"><span class="eyebrow">HABITACIÓN ${r.number}</span><h3>${r.type}</h3><p>${r.capacity} huéspedes · ${r.features.slice(0,4).join(' · ')}</p><div class="features">${r.features.map(x=>`<span class="chip">${x}</span>`).join('')}</div><div class="price">${money(r.price)} <small>/ noche</small></div><div class="room-actions"><button class="btn btn-gold" onclick="openBooking('${r.id}')">Reservar</button></div></div></article>`).join('')||'<div class="empty">No hay habitaciones disponibles.</div>'}
$('#availabilityForm').addEventListener('submit',e=>{e.preventDefault();const a=$('#checkin').value,b=$('#checkout').value,g=+$('#guests').value;if(!a||!b||b<=a){$('#availabilityMessage').textContent='Selecciona fechas válidas.';return}const list=rooms.filter(r=>r.capacity>=g);renderRooms(list);$('#availabilityMessage').textContent=`Mostrando ${list.length} opciones para ${g} huésped(es).`});
function openBooking(roomId){const r=rooms.find(x=>x.id===roomId);$('#modal').innerHTML=`<div class="modal-backdrop"><div class="modal"><div class="modal-head"><div><span class="eyebrow">RESERVA ONLINE</span><h2>${r.type} · Habitación ${r.number}</h2></div><button class="close" onclick="closeModal()">×</button></div><form id="bookForm"><div class="form-grid"><label>Nombre completo<input name="name" required></label><label>Documento<input name="document" required></label><label>Correo<input type="email" name="email" required></label><label>Teléfono<input name="phone" required></label><label>Entrada<input type="date" name="checkin" value="${$('#checkin').value}" required></label><label>Salida<input type="date" name="checkout" value="${$('#checkout').value}" required></label><label>Huéspedes<input type="number" name="guests" min="1" max="${r.capacity}" value="${$('#guests').value||2}" required></label><label>Nacionalidad<input name="nationality" placeholder="Colombiana"></label></div><div class="notice">No necesitas crear una cuenta. La solicitud será registrada como reserva por Internet.</div><div class="modal-actions"><button type="button" class="btn btn-outline" onclick="closeModal()">Cancelar</button><button class="btn btn-gold">Confirmar reserva</button></div></form></div></div>`;$('#bookForm').addEventListener('submit',async e=>{e.preventDefault();const f=new FormData(e.target),d=Object.fromEntries(f);d.rooms=[r.id];const res=await fetch('/api/reservations',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(d)});const out=await res.json();if(!res.ok){alert(out.error);return}closeModal();alert(`Reserva ${out.item.id} registrada correctamente. Estado: Pendiente.`);loadRooms()})}function closeModal(){$('#modal').innerHTML=''}loadRooms();
